# Anaj Bahi sync backend — copy to backend/.env and fill in.
# backend/.env is gitignored; never commit a real token.

DATABASE_URL=sqlite:///./data/anaj.db
DEVICE_TOKEN=change-me-to-a-long-random-string
