.next/
out/
node_modules/

# Firebase env — NEVER commit real config/secrets (Phase 6+)
.env.local
.env*.local

# Firebase emulator transient artifacts (from emulators:exec during rules/E2E runs)
firebase-debug.log
firestore-debug.log
ui-debug.log
.firebase/
